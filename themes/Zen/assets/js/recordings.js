document.addEventListener('DOMContentLoaded', function (event) {

    document.querySelectorAll(".recordings-recording video").forEach((e) => {
        //console.log(e)
        //videojs(e.id).mobileUi()
    })

})